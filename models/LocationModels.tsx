export interface SelectedLocation {
  id: string;
  name: string;
  bu: string;
  flocCode: string;
}

export interface LocationOption {
  id: string;
  name: string;
  bu: string;
  flocCode: string;
}